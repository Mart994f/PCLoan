@model PCLoan.Presentation.Web.Models.UserModel

@{
    ViewData["Title"] = "Login";
}

<h2>Log ind</h2>
<div id="loginbox" style="display:block">
    <div>
        @using (Html.BeginForm("Login", "Login", FormMethod.Post))
        {
            <form name="login" action="" method="post">
                <div>
                    @Html.LabelFor(m => m.UserName)
                    <center>@Html.TextBoxFor(m => m.UserName, new { @class = "formname" })</center>
                </div>
                <div>
                    @Html.LabelFor(m => m.Password)
                    <center>@Html.PasswordFor(m => m.Password, new { @class = "formpass" })</center>
                    <div>
                        @Html.ValidationMessageFor(m => m.UserName, "", new { @class = "text-danger" })
                    </div>
                    <div>
                        @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
                    </div>
                    <div class="lgnfail text-danger">
                        @ViewBag.FailedLogin
                        <!--Validation message for failed login, DONT specify which one is false!-->
                    </div>
                </div>
                <div id="login-btn">
                    <center><button type="submit">login</button></center>
                </div>
            </form>
        }
    </div>
</div>